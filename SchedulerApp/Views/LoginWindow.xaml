<Window x:Class="SchedulerApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <ComboBox Grid.Row="0" HorizontalAlignment="Left" x:Name="LanguageSelector" Width="70" Margin="5" SelectionChanged="LanguageSelector_SelectionChanged">
            <ComboBoxItem Content="English" Tag="en-US" IsSelected="True"/>
            <ComboBoxItem Content="Español" Tag="es-ES"/>
        </ComboBox>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8" Height="25">
            <Label x:Name="lblUsername" Width="80"/> 
            <TextBox x:Name="txtUsername" Width="250"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,8" Height="25">
            <Label x:Name="lblPassword" Width="80"/>
            <PasswordBox x:Name="txtPassword" Width="250"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Height="25">
            <Button x:Name="btnLogin" Width="100" Click="btnLogin_Click"/>
        </StackPanel>

        <TextBlock x:Name="txtStatus" Grid.Row="0" TextWrapping="Wrap" Foreground="Red" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</Window>
